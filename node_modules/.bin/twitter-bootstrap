#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../twitter-bootstrap-node/bin/twitter-bootstrap" "$@"
  ret=$?
else 
  node  "$basedir/../twitter-bootstrap-node/bin/twitter-bootstrap" "$@"
  ret=$?
fi
exit $ret
